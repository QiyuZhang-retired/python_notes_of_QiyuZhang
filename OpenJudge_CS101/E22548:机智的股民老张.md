# E22548:机智的股民老张
**算法核心：贪心算法**  
不断更新当前最小数进行计算
```python
a=list(map(int,input().split()))

output=0
min_num=a[0]
for i in range(len(a)):
    min_num=min(min_num,a[i])
    output=max(a[i]-min_num,output)

print(output)
```
http://cs101.openjudge.cn/pctbook/E22548/
