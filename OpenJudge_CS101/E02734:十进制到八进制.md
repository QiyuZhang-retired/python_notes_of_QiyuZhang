# E02734:十进制到八进制
**核心算法：while循环**
```python
a=int(input())

if a==0:
    print(0)
else:
    trans_a=''
    while a!=0:
        trans_a+=str(a % 8)
        a//=8

    print(trans_a[::-1])
```
http://cs101.openjudge.cn/pctbook/E02734/
