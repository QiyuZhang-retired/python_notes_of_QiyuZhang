# E02808:校门外的树
**算法核心：区间合并**  
数据比较少，可以用列表来进行模拟
```python
L,M=map(int,input().split())
trees=list(range(L+1))

#逐个剔除区间
for _ in range(M):
    a,b=map(int,input().split())
    trees=list(x for x in trees if not a<=x<=b)

print(len(trees))
```
http://cs101.openjudge.cn/pctbook/solution/49939021/
