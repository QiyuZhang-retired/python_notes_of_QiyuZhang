# E02753:菲波那契数列
**核心算法：动态规划**
```python
n=int(input())
nums=[int(input()) for _ in range(n)]

F=[0]*(max(nums)+1)
F[1],F[2]=1,1
for i in range(3,max(nums)+1):
    F[i]=F[i-1]+F[i-2]

for num in nums:
    print(F[num])
```
http://cs101.openjudge.cn/pctbook/E02753/
